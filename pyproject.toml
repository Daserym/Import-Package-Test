[project]
name = "import-package-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "package-test",
]

[tool.uv.sources]
package-test = { git = "https://github.com/Daserym/Package-Test.git" }
